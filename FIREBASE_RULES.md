# Firebase Realtime DB Rules\n\n```\n{\n  \"rules\": {\n    \"users\": {\n      \"$uid\": {\n        \".read\": \"auth != null && auth.uid == $uid\",\n        \".write\": \"auth != null && auth.uid == $uid\",\n        \"bio\": {\n          \".validate\": \"newData.isString() && newData.val().length <= 500\"\n        },\n        \"urls\": {\n          \".validate\": \"newData.isMap()\"\n        },\n        \"avatar\": {\n          \".validate\": \"newData.isString() && newData.val().length <= 1000\"\n        },\n        \"pos\": {\n          \".validate\": \"newData.isArray() && newData.size() === 3 && newData.val().every(v => newData.child(v).isNumber() && v >= -100 && v <= 100)\"\n        },\n        \"points\": {\n          \".validate\": \"newData.isNumber() && newData.val() >= 0\"\n        }\n      }\n    }\n  }\n}\n```\n\n# Firebase Storage Rules\n\n```\nservice firebase.storage {\n  match /b/{bucket}/o {\n    match /avatars/{uid} { allow read, write: if request.auth != null && request.auth.uid == uid; }\n  }\n}\n```\n\nCopy to Firebase Console > Realtime Database > Rules, and Storage > Rules.